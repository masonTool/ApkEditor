apply plugin: 'groovy'

dependencies {
    compile gradleApi()
    compile localGroovy()
}
sourceSets {
    main {
        groovy.srcDirs = ["../apkeditor/src/main/groovy"]
        resources.srcDirs = ["../apkeditor/src/main/resources"]
    }
}
